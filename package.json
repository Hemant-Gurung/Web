{
  "name": "restaurant-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:my-restaurant": "npm run dev --workspace=my-restaurant",
    "dev:verde-kitchen": "npm run dev --workspace=verde-kitchen"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "~5.6.3"
  }
}
